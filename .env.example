# Amplify API (optional; included for parity with other assignments)
AMPLIFY_API_KEY=your_key_here
AMPLIFY_API_URL=https://your-amplify-endpoint/chat/completions
AMPLIFY_MODEL=4o-mini
AMPLIFY_HEADER_NAME=Authorization

# App settings
PORT=3400

# Runtime storage for mutable files (responses.json, extracted_summaries.json, users.json)
# On Railway, mount a persistent volume and set RUNTIME_DATA_DIR to that mount (for example /data)
RUNTIME_DATA_DIR=./data
# Source directory for uploaded CSVs (qa_pairs.csv + extracted_insights.csv).
# If not set and RUNTIME_DATA_DIR is set (Railway), the app defaults SOURCE_DATA_DIR to RUNTIME_DATA_DIR.
# SOURCE_DATA_DIR=./data

# Authentication (required for expert login workflow)
# Generate AUTH_SECRET with: openssl rand -hex 32
AUTH_SECRET=replace_with_long_random_secret
# Option 1 (recommended): bootstrap an admin account, then create expert users in the UI.
# Do NOT set AUTH_USERS/AUTH_USERS_JSON if you want UI-managed users (stored in users.json).
AUTH_BOOTSTRAP_ADMIN_USERNAME=admin
AUTH_BOOTSTRAP_ADMIN_DISPLAY_NAME=Admin
AUTH_BOOTSTRAP_ADMIN_PASSWORD=ChangeMeNow123!

# Option 2: quick static users, comma-separated username:password pairs (UI user management disabled)
# AUTH_USERS=expert1:ChangeMe123,expert2:ChangeMe456
# AUTH_ADMIN_USERS=expert1

# Option 3: JSON array (supports hashed passwords from npm run hash-password -- "<password>")
# AUTH_USERS_JSON=[{"username":"expert1","displayName":"Dr. Expert","passwordHash":"pbkdf2$sha256$...","role":"admin"}]
AUTH_SESSION_DAYS=14
AUTH_COOKIE_NAME=hev_session
